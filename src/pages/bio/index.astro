---
import Base from "../../layouts/Base.astro";
import { projects, bonusExperience } from "../../data/projects";
import { sortProjectsDesc } from "../../lib/timeline";

const timeline = sortProjectsDesc(projects.filter((p) => !p.hidden));
---

<Base
  title="My Bio · Edgar Kretschmann"
  description="Career journey of Edgar Kretschmann, newest to oldest, from marketing management today back to founding a German school in Manila."
  section="bio"
>
  <main class="wrap">
    <a href="/" class="back-link" data-reveal="fade-up">← Home</a>

    <section class="hero" data-reveal="fade-up">
      <div class="avatar">
        <img src="/images/bio/image01.jpg" alt="Edgar Kretschmann" />
      </div>
      <h1>My bio</h1>
    </section>

    <ol class="timeline">
      {
        timeline.map((project) => (
          <li>
            <a class="entry" href={`/projects/${project.slug}`} data-reveal="fade-up" data-reveal-group="bio-timeline">
              <span class="entry-period">{project.bioPeriod}</span>
              <span class="entry-thumb">
                <img src={project.bioImage} alt={project.company} />
              </span>
              <span class="entry-info">
                <span class="entry-role">{project.bioRole}</span>
                <span class="entry-title">
                  {project.company}
                  {project.status === "next" && <span class="status-badge">In progress</span>}
                </span>
                <span class="entry-desc">{project.bioDescription}</span>
              </span>
              <span class="entry-arrow">→</span>
            </a>
          </li>
        ))
      }
      <li>
        <a class="entry entry--bonus" href="/bio/teacher" data-reveal="fade-up" data-reveal-group="bio-timeline">
          <span class="entry-period">{bonusExperience.period}</span>
          <span class="entry-thumb">
            <img src={bonusExperience.image} alt={bonusExperience.company} />
          </span>
          <span class="entry-info">
            <span class="entry-role">{bonusExperience.role}</span>
            <span class="entry-title">Bonus: Language teacher</span>
            <span class="entry-desc">{bonusExperience.description}</span>
          </span>
          <span class="entry-arrow">→</span>
        </a>
      </li>
    </ol>

    <footer data-reveal="fade-up">
      <div class="footer-grid">
        <div>
          <h2>Get in touch</h2>
          <p>hello@edgarkretschmann.com</p>
        </div>
        <div class="footer-actions">
          <a href="mailto:hello@edgarkretschmann.com" class="btn-pill btn-accent">Email me</a>
          <a href="/projects#touch" class="btn-pill btn-ghost">More details</a>
        </div>
      </div>
    </footer>
  </main>
</Base>

<style>
  .wrap {
    max-width: 900px;
    margin: 0 auto;
    padding: var(--space-6) var(--space-4) var(--space-8);
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .hero {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: var(--space-6) 0 var(--space-5);
  }

  .avatar {
    width: 6rem;
    height: 6rem;
    border-radius: 999px;
    overflow: hidden;
    border: 1px solid var(--line);
    margin-bottom: var(--space-4);
  }

  .avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero h1 {
    font-size: clamp(2.2rem, 6vw, 3.4rem);
    font-weight: 500;
    margin: var(--space-2) 0 0;
  }

  .timeline {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    border-top: 1px solid var(--line);
  }

  .entry {
    display: grid;
    grid-template-columns: 90px 104px 1fr auto;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-5) 0;
    border-bottom: 1px solid var(--line);
    text-decoration: none;
    color: inherit;
    transition: opacity 0.2s ease;
  }

  a.entry:hover {
    opacity: 0.6;
  }

  .entry-period {
    font-size: 0.8rem;
    color: var(--text-muted);
    font-weight: 600;
  }

  .entry-thumb {
    width: 104px;
    aspect-ratio: 1;
    border-radius: 6px;
    overflow: hidden;
    background: var(--bg-2);
  }

  .entry-thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .entry-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .entry-role {
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--accent);
    font-weight: 600;
  }

  .entry-title {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    flex-wrap: wrap;
    font-size: 1.3rem;
    font-weight: 500;
  }

  .status-badge {
    font-size: 0.62rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--accent);
    border: 1px solid var(--accent);
    border-radius: 999px;
    padding: 2px 9px;
  }

  .entry-desc {
    color: var(--text-muted);
    font-size: 0.88rem;
    line-height: 1.5;
    margin-top: 4px;
    max-width: 52ch;
  }

  .entry-arrow {
    font-size: 1.3rem;
    color: var(--text-muted);
  }

  footer {
    padding-top: var(--space-6);
    border-top: 1px solid var(--line);
  }

  .footer-grid {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-4);
    flex-wrap: wrap;
  }

  footer h2 {
    font-size: clamp(1.6rem, 4vw, 2.2rem);
    font-weight: 500;
    margin: 0 0 4px;
  }

  footer p {
    color: var(--text-muted);
    margin: 0;
    font-size: 0.9rem;
  }

  .footer-actions {
    display: flex;
    gap: var(--space-3);
    flex-wrap: wrap;
  }

  @media (max-width: 700px) {
    .entry {
      grid-template-columns: 1fr;
      text-align: left;
    }
    .entry-thumb {
      width: 100%;
      max-width: 220px;
      aspect-ratio: 1;
    }
    .entry-arrow {
      display: none;
    }
  }
</style>
